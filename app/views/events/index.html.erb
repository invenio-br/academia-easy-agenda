<div class="w-100 d-flex">
  <h1 class="flex-grow-1">Eventos</h1>
  <div>
    <%= link_to "Novo Evento", new_event_path, class: "btn btn-primary" %>
  </div>
</div>

<div class="d-flex flex-column gap-4">
  <%= render @events %>
</div>
